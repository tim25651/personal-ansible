# Descriptions: Install apt packages

# Add custom packages to install
- import_tasks: keyrings.yml

- name: Update and upgrade apt packages
  become: yes
  apt:
    update_cache: yes
    upgrade: dist

- name: Install required packages
  become: yes
  apt:
    name:
      - git
      - curl
      - zsh
    state: present
